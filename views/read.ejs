<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Files</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="stylesheets/style.css">
</head>
<body>
    <div class="w-full h-full p-5 text-white bg-zinc-800">

        <div class="mb-10">
            <a href="/" class="text-blue-400 ">Back to Home</a>
        </div>

        <h1 class="text-2xl mb-2 tracking-tighter">All Users</h1>

        <div class="Users flex gap-5 flex-wrap">

            <% if (users.length > 0) { %> <!-- Corrected this line -->
                <% users.forEach((user) => { %> <!-- Corrected this line -->
                    <div class="user bg-zinc-600 p-4 rounded-md w-52">
                        <div class="w-full h-36 bg-zinc-500 overflow-hidden rounded-md">
                            <img class="w-full h-full object-cover object-center" src="<%= user.image %>" alt=""> <!-- Removed extra space -->
                        </div>
                        <h3 class="text-xl tracking-tighter mt-2"><%= user.name %></h3>
                        <h5 class="email text-zinc-500"><%= user.email %></h5>
                        
                        <div class="btns flex gap-24 mt-4">
                            <a href="/edit/<%= user._id %>" class="text-zinc-200 text-sm">Edit User</a>
                            <a href="/delete/<%= user._id %>" class="text-red-700 text-sm">Delete User</a> <!-- Removed extra space -->
                        </div>
                    </div>
                <% }) %>
            <% } else { %> <!-- Corrected this line -->
                <h3 class="text-zinc-600">No users yet</h3>
            <% } %> <!-- Corrected this line -->

        </div>

    </div>
</body>
</html>
